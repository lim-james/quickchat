<li class="chat" data-time="<%= chat.created_at.to_i %>">
    <small class="text-muted">
        <%= chat.sender %> <i><%= chat.created_at.localtime.strftime("at %I:%M%p") %></i><br>
    </small>
    <b class="h4"><%= chat.content %></b>
</li>
 
